On claude.ai or in the Claude desktop app, add trenuj.se as a custom connector:

1. Open **Settings → Connectors** and click **Add custom connector**.
2. Paste the server URL:

```
{SERVER_URL}/mcp
```

3. On first use, Claude opens a login window — sign in with your trenuj.se account and approve access.
