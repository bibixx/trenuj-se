name = "trenuj-se"
compatibility_date = "2026-04-01"
main = "server/worker.ts"

[assets]
directory = "dist"
not_found_handling = "single-page-application"

[vars]
# Override locally via `.dev.vars`.
PUBLIC_APP_URL = "https://www.trenuj.se"
